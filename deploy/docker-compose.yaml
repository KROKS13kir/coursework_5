version: '3.8'

services:

  app:
    build: . .
    command: waitress-serve --listen=*:8000 myapp.wsgi:application
    restart: always

  nginx:
    image: nginx:1.19.3
    ports:
      - 25000:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: always
